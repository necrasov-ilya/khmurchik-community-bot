bot:
  token: ""
  target_chat_id: 0

server:
  poll_timeout: 30

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  name: "khmurchik_bot"
  ssl_mode: "disable"
  max_open_conns: 25
  max_idle_conns: 10
  conn_max_lifetime: "5m"

scheduler:
  timezone: "Europe/Minsk"
